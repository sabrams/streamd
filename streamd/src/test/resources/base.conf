streamd {
    version = "0.1"
    modules = ["com.appendr.streamd.util.TestCEPModule"]

    control {
        port = 7777
    }

    zookeeper {
        hosts = "localhost:2181"
        path = "/stream-daemon"
        timeout = 30000
    }
}